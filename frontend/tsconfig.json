{
  // Minimal and compatible with current Nuxt output
  // Nuxt generates .nuxt/tsconfig.json and .nuxt/tsconfig.server.json
  // Use extends instead of broken references to non-existent files
  "extends": "./.nuxt/tsconfig.json"
}
